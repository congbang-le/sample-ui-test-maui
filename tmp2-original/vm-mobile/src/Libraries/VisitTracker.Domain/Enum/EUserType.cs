namespace VisitTracker.Domain;

/// <summary>
/// Enumeration representing different user types in the system.
/// This enum is used to categorize users based on their roles.
/// </summary>
public enum EUserType
{
    CAREWORKER = 1,
    SERVICEUSER = 2,
    NEXTOFKIN = 3,
    SUPERVISOR = 4
}