namespace VisitTracker;

/// <summary>
/// EPlatform is an enumeration that defines the different platforms that the application can run on.
/// Each platform corresponds to a specific operating system or environment that the application is designed to support.
/// </summary>
public enum EPlatform
{
    Android = 0,
    iOS = 1,
}