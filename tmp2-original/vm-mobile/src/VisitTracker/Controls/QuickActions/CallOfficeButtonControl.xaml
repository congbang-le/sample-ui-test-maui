<?xml version="1.0" encoding="utf-8" ?>
<local:BaseContentView
    x:Class="VisitTracker.CallOfficeButtonControl"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:VisitTracker"
    xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
    x:Name="CallOffice">
    <ContentView.ControlTemplate>
        <ControlTemplate>
            <material:ButtonView
                AutomationId="CallOfficeButton"
                Style="{StaticResource QuickActionsButtonStyle}"
                TappedCommand="{Binding Source={x:Reference CallOffice}, Path=OpenCallOfficeCommand}">
                <HorizontalStackLayout Spacing="2">
                    <Label Style="{StaticResource QuickActionsIconStyle}" Text="{x:Static local:MaterialCommunityIconsFont.Phone}" />

                    <Label Style="{StaticResource QuickActionsTextStyle}" Text="Office" />
                </HorizontalStackLayout>
            </material:ButtonView>
        </ControlTemplate>
    </ContentView.ControlTemplate>
</local:BaseContentView>