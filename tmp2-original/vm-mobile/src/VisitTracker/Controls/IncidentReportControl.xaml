<?xml version="1.0" encoding="utf-8" ?>
<local:BaseContentView
    x:Class="VisitTracker.IncidentReportControl"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:domain="clr-namespace:VisitTracker.Domain;assembly=VisitTracker.Domain"
    xmlns:local="clr-namespace:VisitTracker"
    xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
    x:Name="this">
    <ContentView.ControlTemplate>
        <ControlTemplate>
            <VerticalStackLayout>
                <Label
                    HorizontalOptions="Start"
                    HorizontalTextAlignment="Start"
                    Style="{StaticResource Heading1Style}"
                    Text="Incident Reports" />

                <VerticalStackLayout Margin="0,5" Spacing="10">
                    <HorizontalStackLayout BindableLayout.ItemsSource="{Binding IncidentReportList, Mode=TwoWay, Source={x:Reference this}}" Spacing="10">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate x:DataType="domain:Incident">
                                <material:ButtonView
                                    Padding="10,3"
                                    CommandParameter="{Binding .}"
                                    Style="{StaticResource BtnDefault}"
                                    TappedCommand="{Binding OpenByIncidentCommand, Source={x:Reference this}}">
                                    <Label
                                        Grid.Column="0"
                                        HorizontalOptions="Start"
                                        HorizontalTextAlignment="Start"
                                        LineBreakMode="TailTruncation"
                                        Style="{StaticResource Heading2Style}"
                                        Text="{Binding DisplayName}"
                                        VerticalOptions="Fill"
                                        VerticalTextAlignment="Center" />
                                </material:ButtonView>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </VerticalStackLayout>
        </ControlTemplate>
    </ContentView.ControlTemplate>
</local:BaseContentView>